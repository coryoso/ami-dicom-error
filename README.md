# AMI dicom loading error

Repository to discuss a problem loading a dicom file

## Installation
1. Clone repo
2. Install packages using yarn by using command ```yarn```
3. Run with ```yarn start```
4. Open browser at ```localhost:1234```
